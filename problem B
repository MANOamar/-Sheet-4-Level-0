#include<iostream>
#include<string>
using namespace std;
int main()
{
	string S; int i = 0;
    getline(cin,S);
	for (i = 0; i < S.size(); i++)
	{ 
		if (S[i] == 92)
			return 0;
		else
			cout << S[i];
	}
	
	return 0;
}
